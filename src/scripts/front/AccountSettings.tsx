import React from "react"
import { useEffect, useState } from "react"

const AccountSettings = () => {
    return (
        <div className="settings-button button" onClick={()=>{console.log('Acctoun')}}>
            A
        </div>
    )
}

export default AccountSettings